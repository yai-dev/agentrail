---
"@agentrail/config": minor
"@agentrail/deep-research": patch
"@agentrail/orchestration": patch
"@agentrail/sandbox": patch
---

Add shared YAML configuration support

Migrate the bundled examples to config/agentrail.yaml, move sandbox and sub-agent worker runtime knobs onto explicit config inputs, and keep the sandbox release flow aligned with the published GHCR image.
