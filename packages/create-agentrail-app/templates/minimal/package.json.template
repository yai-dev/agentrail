{
  "name": "{{PROJECT_NAME}}",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "node --import=tsx/esm --watch src/main.ts",
    "start": "node dist/main.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@agentrail/runtime-core": "{{VERSION_AGENTRAIL_RUNTIME_CORE}}",
    "@agentrail/host": "{{VERSION_AGENTRAIL_HOST}}",
    "@agentrail/memo": "{{VERSION_AGENTRAIL_MEMO}}",
    "@agentrail/sandbox": "{{VERSION_AGENTRAIL_SANDBOX}}",
    "hono": "{{VERSION_HONO}}",
    "@hono/node-server": "{{VERSION_HONO_NODE_SERVER}}"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
